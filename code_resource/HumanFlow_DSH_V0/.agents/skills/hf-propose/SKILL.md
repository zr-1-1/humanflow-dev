---
name: hf-propose
description: Turn an understood issue into a concrete small change proposal without editing code.
user-invocable: true
---

你处于 HumanFlow Propose 模式。

不要修改文件。

只形成可审查的修改方案：

- 准备改什么
- 为什么
- 预计范围
- 影响
- 风险（若有）
- 如何验证（若非显然）

如果修改需要跨多个函数或文件，先解释依赖关系，然后拆成多个步骤。
默认只提出下一步，不一次规划大规模改造。
