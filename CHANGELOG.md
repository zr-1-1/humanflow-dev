# 更新日志

本文档记录 HumanFlow 各版本对外可见的变化。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## [0.4.2] - 2026-09-24

### 变更

- 面板整体接入仓库内的 UI 素材库（`media/ui/HumanFlow_UI_Asset_Library_V2`）：Design Token、组件样式、HumanFlow 专属图标与空状态插画，不再由面板自行拼接颜色与控件样式。
- 候选批次改为「提案卡 + 变更摘要 + 文件状态芯片 + 候选片段」结构，底部固定审查栏显示勾选数量、文件导航与「应用并保存勾选修改」。
- 项目问题改为摘要优先的卡片：编号、处理状态、位置、依据与影响分列，允许就地讨论或只处理该问题。
- 历史批次、验证记录、固定决策、关注范围与上下文用量改为卡片与进度条呈现；上下文用量显示讨论历史、当前代码与任务状态的分项占比，不再只给一个总数。
- 空状态改为设计系统插画：无讨论、无候选、候选失效分别使用对应文案与插画。

### 新增

- 人工检查点：模型给出候选后说明涉及的文件与片段数量，并提供「先讨论 / 查看候选」，不提供一键全改。
- 面板头部状态芯片：生成中、待审查、已应用、候选已失效等状态直接可见。
- 候选预览提供两种方式：`对比预览（双栏）` 打开 VS Code 原生 Diff；`单页改动（整份文件）` 在一个只读页面里显示**整份候选代码**——按目标文件语言语法高亮（类名、函数名、关键字都有颜色），删除行与新增行就地插入并以主题色背景和 `−` / `+` 标记区分。两者共用同一个预览槽，切换时自动回收上一个页面。
- 说明：VS Code 没有"打开内联 Diff"的公开参数，编辑器菜单里的 Inline View 实际会改写用户设置 `diffEditor.renderSideBySide`，本扩展不替用户改设置；需要 VS Code 原生内联视图时，可在 Diff 编辑器右上角菜单选择 Inline View（或设置该项）。
- 单页改动补齐三处体验：变量/属性/函数/类型名按主题 `symbolIcon.*` 颜色着色（虚拟文档没有语言服务器语义高亮，这里用轻量标注补足，且不作用于 HTML/JSON/YAML 等标记与数据格式，以免覆盖标签、属性与键的原有着色）；滚动条 Overview Ruler 用 diff 颜色标出删除/新增行位置；打开后自动定位到第一处改动。
- 修正单页改动的滚动条标记：VS Code 的 `diffEditorOverview.*` 颜色在注册表里默认是 null（主题未定义即无颜色），现将删除/新增行的侧边标记改用与原生 Diff 相同的回退颜色（`diffEditor.removedTextBackground` / `insertedTextBackground`，淡红 / 淡绿），与对比视图一致。
- 前后一致的片段（模型给出的空改动）不再在单页改动里显示成"删除 + 新增"，只保留真实改动。
- 离线界面检查新增设计系统接线测试与面板标记一致性测试；浏览器 UI 脚本改为加载设计系统样式，并新增横向溢出与组件结构断言。

### 修复

- 面板输入框按浏览器默认 `content-box` 计算宽度，导致目录搜索框横向溢出并出现横向滚动条，现统一为 `border-box`。
- 窄面板下任务标题被操作按钮挤出可视区，审查栏按钮压住候选信息，现改为分行布局。
- 模型把 Markdown 转义习惯写进 JSON（例如 `src/stk\_export/stk\_out.py`）会让整批响应被判为“不是有效 JSON”：现在只对路径、命令、引用、说明等非代码字段去掉 Markdown 风格的多余反斜杠；候选代码片段内出现非法转义、或出现 `\d` 这类可能有语义的反斜杠时仍然拒绝，并提示重新生成，不改写要写入文件的内容。
- `dependencies` / `references` 写成一段文字而不是数组时按单项列表接收，不再整批失效；提示词同时明确二者为字符串数组，并要求路径与文字不做 JSON/Markdown 转义。
- 发生上述规范化时，候选卡片会显示修复数量，避免静默改写模型响应。
- 面板重构时漏绑“更新关注点（保留讨论）”按钮：无选区点击后不再把关注点更新为当前整个文件（后端逻辑未变，是事件未发出）；已恢复绑定，并加入面板事件接线回归测试。
- 面板重构时漏掉任务标题同步，标题始终显示默认值；现按任务标题实时更新。
- 候选片段缺少列定义，代码被压缩到 18px 窄列；同时补上固定决策列表间距，并清理未使用的样式规则。
- 预览只保留一个 VS Code 预览标签页：再次预览会先回收上一次的临时文档，关闭面板再重开不会出现"旧预览 + 新预览"两个页面；标题带上文件名，窗口重载后失效的预览会显示明确说明而不是空白文件。
- 修复“装好新版本但没重新加载窗口”时的错乱：那时面板脚本已是新版、扩展宿主仍是旧代码，点「单页改动」会退回旧的双栏对比。面板与宿主现在交换协议号，不一致时面板给出提示并隐藏宿主不支持的新入口，提示执行“开发人员：重新加载窗口”。

### 验证

- 58 项离线测试通过，其中新增面板标记一致性、设计系统接线、非法转义修复与提示词约束检查。
- 面板在 1080×800 与 380×740 完成 100 轮交互检查，覆盖设计系统加载、空状态、状态芯片、审查栏勾选同步与横向溢出。
- 15 个真实 VS Code 宿主场景通过；本版安装包为 `dist/humanflow-0.4.2.vsix`，旧包保留。

## [0.4.1] - 2026-09-24

### 变更

- 任务目标、固定决策与线程策略收进默认折叠的“高级设置（可选）”，不填写任何设置也能直接提问和接受修改。
- 额外修改限制默认关闭：启用后只检查已填写的路径或数字，留空表示不额外限制。
- 未启用额外限制时沿用基础校验：每批最多 12 个文件、候选片段合计不超过 100000 字符，并保留路径与文件版本检查。
- 旧版本自动写入的默认预算在升级后自动关闭，用户自定义过的限制保留。

### 修复

- 任务目标、固定决策和未解决问题改为每轮完整携带，不再因历史裁剪而丢失。

### 验证

- 52 项离线测试通过。
- 面板在 1080×800 与 380×740 两种尺寸下完成 100 轮交互检查。

## [0.4.0] - 2026-09-24

### 新增

- 讨论、修改、问题与验证三个工作区视图，支持全文历史搜索与按用户请求定位。
- 任务草稿、阅读位置与折叠状态在面板重开后恢复。
- “本轮上下文与 Harness 用量”面板，展示分项字符数、文件内容版本与历史省略情况。
- 任务级固定目标与固定决策，可编辑、查看来源轮次、取消固定。
- 修改预算：允许路径、文件数、替换前后行数，在应用前强制检查。
- 验证命令与批次、轮次、代码版本关联，代码变化后标记为需要重新验证。
- 持续线程与 Harness 压缩作为可回退的实验选项，默认关闭。

## [0.3.0] - 2026-09-24

### 新增

- DeepSeek 官方 Codex 兼容接入：任务级服务切换、密钥存入 VS Code SecretStorage、独立子进程 provider 配置与模型目录。

### 验证

- 33 项离线测试与 14 个宿主场景通过。
- 已验证真实 Codex 到本机 Responses 替身的请求路由；DeepSeek 远程推理未验证，独立 DSH 后端未实现。
- 0.3.1–0.3.3 为内部迭代构建，未单独记录变更。

## [0.2.0] - 2026-09-24

### 新增

- 项目问题列表与状态流转，可由单个问题生成关联修改批次。
- 依赖说明、可编辑候选草稿、生成前文件元数据基线。
- 可选验证命令及结果记录，新增文件支持。
- Node 与 Codex 路径自动发现、VSIX 打包脚本。
- 可选双模型审查：只审查勾选结果，不修改候选、不执行验证。

### 验证

- 30 项离线测试与 13 个宿主场景通过，包含宿主进程退出后的持久化恢复。
- VSIX 已在隔离扩展目录验证安装。

## 0.1.x

首个可用原型：选区讨论、项目级只读读取、跨文件候选建议、原生差异预览、按片段接受与应用后保存。该阶段未发布独立版本记录。

[0.4.2]: https://github.com/zr-1-1/humanflow-dev/releases/tag/v0.4.2
[0.4.1]: https://github.com/zr-1-1/humanflow-dev/releases/tag/v0.4.1
